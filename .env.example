# Get a free API key at https://resend.com after signing up (no credit card required).
RESEND_API_KEY=

# Optional: override which inbox contact form / newsletter submissions are sent to.
# Defaults to SITE.email in src/lib/constants.ts if not set.
CONTACT_TO_EMAIL=
